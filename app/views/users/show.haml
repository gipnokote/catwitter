.row
  .col-md-5
    %h1
      %span.glyphicon.glyphicon-user
      = @user.username
- if @user.real_name
  .row
    .col-md-5
      %h4
        = @user.real_name

- if current_user
  .row.bottom-buffer-40
    .col-md-2
      #friend-button
        = render partial: 'friend_button'
    .col-md-2
      #block-button
        = render partial: 'block_button'

- if current_user && @user.blocking?(current_user)
  You are blocked by this user :(
- else
  - @user.posts.recent.each do |post|
    = render partial: 'posts/post', locals: {post: post}